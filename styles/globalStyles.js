import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    // backgroundColor: '#fff',
  },
  paragraph: {
    marginVertical: 8,
    lineHeight: 20,
  }
});

export default globalStyles;